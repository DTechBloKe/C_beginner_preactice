#include <stdio.h>
//This programe is about simple increament & decreament
int main()
{
	int counter;
	counter +=1;
	printf(" counter at %d\n", counter);
	counter +=1;
        printf(" counter at %d\n", counter);
	counter +=4;
	printf("added 4 to the counter %d\n", counter);
	counter *=2;
	printf("counter multiplied by two %d\n", counter);

	return 0;
}
